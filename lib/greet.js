'use strict';

var greet = module.exports = exports = function(name) {
  // remove this before pushing your code out.
  debugger; /* jshint ignore:line */
  return 'hello' + name;
};

greet('some person');
